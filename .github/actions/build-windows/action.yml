name: Build UTV on Windows
description: Build and test UTV on Windows

inputs:
  build-type:
    required: false
    default: 'Release'
  qt-version:
    required: false
    default: '6.11.0'
  python-version:
    required: false
    default: '3.12'
  cmake-version:
    required: false
    default: '4.3.2'
  vfx-platform:
    required: false
    default: 'CY2026'
  msvc-component:
    required: false
    default: ''
  msvc-compiler:
    required: false
    default: ''
  qt5_modules:
    required: false
    default: ''
  windows_qt5_archives:
    required: false
    default: 'd3dcompiler_47 opengl32sw qt3d qtactiveqt qtbase qtconnectivity qtdeclarative qtgraphicaleffects qtimageformats qtlocation qtmultimedia qtquickcontrols qtquickcontrols2 qtremoteobjects qtscxml qtsensors qtsvg qttools qttranslations qtwebchannel qtwebsockets qtwebview qtwinextras qtxmlpatterns'
  windows_qt6_modules:
    required: false
    default: 'debug_info qt3d qt5compat qtactiveqt qtcharts qtconnectivity qtdatavis3d qtgrpc qthttpserver qtimageformats qtlanguageserver qtlocation qtlottie qtmultimedia qtnetworkauth qtpdf qtpositioning qtquick3d qtquick3dphysics qtquickeffectmaker qtquicktimeline qtremoteobjects qtscxml qtsensors qtserialbus qtserialport qtshadertools qtspeech qtvirtualkeyboard qtwebchannel qtwebengine qtwebsockets qtwebview'
  windows_qt6_archives:
    required: false
    default: 'd3dcompiler_47 opengl32sw qtbase qtdeclarative qtsvg qttools qttranslations'

runs:
  using: "composite"
  steps:
    - name: Download and Install Prebuilt Dependencies
      shell: powershell
      run: |
        $ReleaseUrl = "https://api.github.com/repos/mcoliver4/utv-dependencies/releases/latest"
        $ReleaseData = Invoke-RestMethod -Uri $ReleaseUrl
        $Asset = $ReleaseData.assets | Where-Object { $_.name -like "*.msi" } | Select-Object -First 1
        if (-not $Asset) {
            Write-Error "Could not find MSI asset in the release."
            exit 1
        }
        $DownloadUrl = $Asset.browser_download_url
        Write-Host "Downloading $DownloadUrl..."
        Invoke-WebRequest -Uri $DownloadUrl -OutFile "OpenUTVDeps.msi"
        Write-Host "Installing MSI..."
        # Use Start-Process with /passive for better non-interactive behavior
        $msiProcess = Start-Process msiexec.exe -ArgumentList "/i OpenUTVDeps.msi /qn /passive" -Wait -PassThru -NoNewWindow
        if ($msiProcess.ExitCode -ne 0 -and $msiProcess.ExitCode -ne 3010) {
            Write-Error "MSI installation failed with exit code $($msiProcess.ExitCode)"
            exit 1
        }
        Write-Host "Installation complete."
        $DepsDir = Get-ChildItem -Path "C:\Program Files\OpenUTVDeps *" | Sort-Object Name -Descending | Select-Object -First 1
        if ($DepsDir) {
            Write-Host "Found OpenUTVDeps at $($DepsDir.FullName)"
            $BinPath = Join-Path $DepsDir.FullName "installed\x64-windows\bin"
            echo "$BinPath" >> $env:GITHUB_PATH
        } else {
            Write-Warning "Could not find OpenUTVDeps installation directory."
        }

    - name: Set up MSVC
      uses: ilammy/msvc-dev-cmd@v1
      with:
        toolset: ${{ inputs.msvc-compiler }}
        arch: 'x86_64'

    - name: Configure Bundled Python
      shell: powershell
      run: |
        # Look for Bundled Python in the OpenUTVDeps directory
        $DepsDir = Get-ChildItem -Path "C:\Program Files\OpenUTVDeps *" | Sort-Object Name -Descending | Select-Object -First 1
        if ($DepsDir) {
            $PythonPath = Join-Path $DepsDir.FullName "installed\x64-windows\tools\python3"
            if (Test-Path "$PythonPath\python.exe") {
                Write-Host "Configuring bundled Python at $PythonPath"
                "pythonLocation=$PythonPath" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
                "$PythonPath" | Out-File -FilePath $env:GITHUB_PATH -Append -Encoding utf8
                "$PythonPath\Scripts" | Out-File -FilePath $env:GITHUB_PATH -Append -Encoding utf8
            } else {
                Write-Error "Bundled Python not found at $PythonPath"
                exit 1
            }
        } else {
            Write-Error "OpenUTVDeps not found. Cannot configure bundled Python."
            exit 1
        }

    - name: Display Python installation location
      run: python -c "import sys; print(sys.executable)"
      shell: powershell

    - name: Cache CMake for Windows
      uses: actions/cache@v5
      id: windows-cmake-cache
      with:
        path: "C:/Program Files/CMake"
        key: ${{ runner.os }}-cmake-${{ inputs.cmake-version }}

    - name: Install CMake ${{ inputs.cmake-version }} with chocolatey
      if: steps.windows-cmake-cache.outputs.cache-hit != 'true'
      run: choco install cmake --version=${{ inputs.cmake-version }} --allow-downgrade
      shell: powershell

    - name: Install build tools (JOM, flex, bison, nasm, patch)
      run: |
        choco install jom winflexbison3 nasm patch --yes
        "C:\ProgramData\chocolatey\bin" | Out-File -FilePath $env:GITHUB_PATH -Append -Encoding utf8
      shell: powershell

    - name: Set PATH environment variable
      run: |
        echo "export PATH=\"/c/Program Files/CMake/bin:/c/ProgramData/Chocolatey/bin:/c/ProgramData/Chocolatey/lib/jom/tools:\$PATH\"" >> ~/.bash_profile
      shell: bash

    - name: Display environment variables
      run: |
        echo $PATH
        which python
        python --version
        which cmake
      shell: bash

    - name: Prepare Qt folder
      run: mkdir -p /c/qt
      shell: bash

    - name: Install Qt ${{ inputs.qt-version }}
      uses: jurplel/install-qt-action@v4
      with:
        version: '${{ inputs.qt-version }}'
        host: 'windows'
        target: 'desktop'
        arch: 'win64_msvc2022_64'
        dir: "c:/"
        install-deps: 'false'
        modules: ${{ inputs.windows_qt6_modules }}
        archives: ${{ inputs.windows_qt6_archives }}
        cache: 'true'
        setup-python: 'false'
        tools: 'tools_qtcreator'
        set-env: 'true'
        tools-only: 'false'
        aqtsource: 'git+https://github.com/miurahr/aqtinstall.git@master'

    - name: Install Python dependencies
      run: |
        & "$env:pythonLocation\python.exe" -m pip install --upgrade uv
        & "$env:pythonLocation\Scripts\uv.exe" pip install --system -r "$env:GITHUB_WORKSPACE\utv\requirements.txt"
      shell: powershell

    - name: Setup sccache
      # Used for this run only (SCCACHE_GHA_ENABLED=false); not stored in Actions cache.
      # For sccache to actually cache the Windows/MSVC build, CMake must use it as the compiler
      # launcher, e.g. -DCMAKE_C_COMPILER_LAUNCHER=sccache -DCMAKE_CXX_COMPILER_LAUNCHER=sccache
      # in the Configure UTV step (and sccache must be on PATH when the build runs).
      uses: mozilla-actions/sccache-action@v0.0.10
      with:
        version: "v0.15.0"

    - name: Configure sccache for bash
      run: |
        # sccache is installed to cargo bin
        export PATH="/c/Users/runneradmin/.cargo/bin:$PATH"
        sccache --version || echo "WARNING: sccache not found in PATH"
      shell: bash

    - name: Configure UTV
      run: cmake -B _build -G "Visual Studio 17 2022" -A x64 -DRV_DEPS_WIN_PERL_ROOT=c:/Strawberry/perl/bin -DCMAKE_PREFIX_PATH="C:/Qt/${{ inputs.qt-version }}/msvc2022_64;C:/Qt/${{ inputs.qt-version }}/qt6_6110_msvc2022_64" -DCMAKE_BUILD_TYPE=${{ inputs.build-type }} -DPython3_ROOT_DIR="$env:pythonLocation" -DCMAKE_C_COMPILER_LAUNCHER=sccache -DCMAKE_CXX_COMPILER_LAUNCHER=sccache
      shell: powershell

    - name: Build UTV dependencies
      run: cmake --build _build --config ${{ inputs.build-type }} --target dependencies --parallel=$(& "$env:pythonLocation\python.exe" -c 'import os; print(os.cpu_count())') -v
      shell: powershell

    - name: Build UTV main executable
      run: cmake --build _build --config ${{ inputs.build-type }} --target main_executable --parallel=$(& "$env:pythonLocation\python.exe" -c 'import os; print(os.cpu_count())') -v
      shell: powershell

    - name: Tests
      continue-on-error: true
      run: |
        ls -al _build/stage/app/bin
        ctest --test-dir _build -C ${{ inputs.build-type }} --extra-verbose
      shell: bash

    - name: Install UTV
      run: cmake --install _build --prefix $(pwd)/_install --config ${{ inputs.build-type }}
      shell: bash
